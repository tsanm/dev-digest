/** Skill type → chip colour for the Skills tab. */
export const SKILL_TYPE_COLOR: Record<string, string> = {
  rubric: "#3b82f6",
  convention: "#10b981",
  security: "#ef4444",
  custom: "#999999",
};

/** Fallback chip colour for unknown skill types. */
export const SKILL_TYPE_COLOR_FALLBACK = "#999";
