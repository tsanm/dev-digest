import { SkillsView } from "./_components/SkillsView";

/* Route: /skills (?id, ?import). Thin route entry — the view, its styles,
   constants, helpers and test are colocated under _components/SkillsView. */
export default function SkillsPage() {
  return <SkillsView />;
}
