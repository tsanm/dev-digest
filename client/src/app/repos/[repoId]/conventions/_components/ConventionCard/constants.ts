/** Constants for ConventionCard. */

/** Confidence at/above which a candidate's bar is shown as "ok" (vs "warn"). */
export const HIGH_CONFIDENCE = 0.85;

/** Width (px) of the confidence progress bar. */
export const CONFIDENCE_BAR_WIDTH = 90;

/** Width (px) of the right-hand action column. */
export const ACTION_COL_WIDTH = 150;
